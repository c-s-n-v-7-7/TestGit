#include<bits/stdc++.h>
#include<set>
#include<map>
#include<utility>
using namespace std; 


//INCOMPLETEEEE!!!!!!!!!!!!!!!!!!!!!!!
int main()
{
    int n ;
    cin >> n ;
    int u , v , z ;
    pair<int,int> p ;
    map<int,pair<int,int>> M ;
    for ( int i=0 ; i<n-1 ; i++ )
    {
        cin >> z >> p.first >> p.second ;
    }
    map<int,pair<int,int>> :: iterator i , j , it ;
    advance(it,n-2); 
    for ( i = M.begin() ; i!=M.end() ; ++i )
    {   
        j = i ;
        for ( advance(j,1) ; j!=it ; ++j)
        {
            if ( j->first == )
        }
        
    }
    return 0 ;
}